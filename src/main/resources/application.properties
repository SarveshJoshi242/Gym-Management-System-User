# === Database (set via environment variables in production) ===
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/gym_management?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=$arvesh27
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === Gemini AI API Key ===
gemini.api.key=AIzaSyBvjGi6PpyQoLf4e-c8nf7zkRLpyiEWACI

# === Server Port (Render injects PORT automatically) ===
server.port=${PORT:8080}

# === CORS — Frontend URL (set to your Vercel URL in production) ===
cors.allowed.origin=${FRONTEND_URL:http://localhost:8080}

# === JPA/Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# === Static files & errors ===
spring.mvc.static-path-pattern=/**
server.error.include-message=always

# === JWT (set via environment variables in production) ===
jwt.secret=${JWT_SECRET:GymPlusSecretKeyForJWT2026SuperSecure256Bit!!}
jwt.expiration-ms=${JWT_EXPIRATION:3600000}
